{
	"name": "musicrelease-scss",
	"version": "0.0.1",
	"description": "SCSS compiler using node-sass",
	"devDependencies": {
		"bootstrap": "^5.1.3",
		"sass": "^1.42.1"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"scss": "sass --watch scss:assets/css --style=compressed"
	}
}
